import React from 'react'
import styles from '../../Utils/Styles';

export default () => {
    return (
        <div style={styles.headerContainer}>
            <h1 style={styles.headerText}>Calculate Your Mortgage</h1>
        </div>
    )
}
