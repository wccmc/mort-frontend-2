const routes = [
    'General',
    'Veteran',
    'Financial',
    'Results'
]

const handleNavigation = (current, next, vet) => {
    // simple navigation function
    if(next){

    }

    const location = veteran ? "Veteran" : "Financial";
    // props.navigate(location)
}